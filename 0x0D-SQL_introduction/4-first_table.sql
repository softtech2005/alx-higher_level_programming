-- Create a table called first_table with values id and name in my MySQL server.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;

USE hbtn_0c_0;

CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
